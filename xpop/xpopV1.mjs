import assert from 'assert'

const xpop = async ({
  vl,
  ledger: {
    json,
    binary,
  },
  validations,
  tx,
}) => {
  // console.log(vl, json, binary, validations, tx)
  return '{...xpop...}'
}

export {
  xpop,
}
